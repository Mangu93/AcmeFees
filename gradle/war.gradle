apply plugin: "war"

bootWar {
    mainClassName = "com.amp.acmefees.AcmeFeesApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
